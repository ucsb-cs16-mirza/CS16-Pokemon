#include "main.h"

using namespace std;

// We have a struct defined like so:
/*
struct Encounter {
    string pokemon;
    int number;
};
*/

void catchMe(Encounter* e) {

    //TODO: Write code to initialize the Encounter pokemon to "Chansey" and the number to 113

    POKEBALL // Don't delete this line!
}

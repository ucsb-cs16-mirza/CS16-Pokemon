#include "main.h"

using namespace std;

// We have a struct defined like so:
/*
struct Encounter {
    string pokemon;
    int number;
};
*/

/*
    Write a function called catchMe(), which has no return value, and accepts
    a pointer to an Encounter "e", a string "pokemon", and an int "number",
    (in that order). Initialize the Encounter's pokemon to the pokemon string, 
    and initialize the Encounter's number to be the number int parameter
*/

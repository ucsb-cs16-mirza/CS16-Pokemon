#include "main.h"

using namespace std;

// We have a struct defined like so:
/*
struct Encounter {
    string pokemon;
    int number;
};
*/

uint32 catchMe(Encounter& e) {

    //TODO: Write one line to get the address of the Encounter's number and store it in a pointer called "ptr"
    
    //TODO: Write another line to dereference "ptr" and set the value to 58

    POKEBALL // Don't delete this line!
}
